const nota = 50

if (nota >= 80) {
    console.log("Parabéns, você foi aprovada(o)")
} else if (nota < 80 && nota >= 60) {
    console.log("Vocẽ está na lista de espera")
} else {
    console.log("Vocẽ foi reprovada(o)")
}